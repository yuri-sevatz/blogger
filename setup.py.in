#!/usr/bin/env python

from distutils.core import setup

setup(
    name='blogger_dl',
    version='${PACKAGE_VERSION}',
    package_dir={'': '${CMAKE_CURRENT_SOURCE_DIR}'},
    packages=['blogger_dl'],
    scripts=['${CMAKE_CURRENT_SOURCE_DIR}/blogger-dl'],
)
